# FIY PWA Environment Variables
# Copy this to .env and fill in the values.

# API base URL (no trailing slash)
VITE_API_BASE=https://api.fiy.app

# Public-facing API key (v1 — will be replaced with proper auth later)
VITE_API_KEY=your-api-key-here
